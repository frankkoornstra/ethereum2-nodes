apiVersion: 1

providers:
  - name: Default
    folder: Default
    folderUid: 'default'
    type: file
    disableDeletion: true
    editable: false
    options:
      path: /provisioning/dashboards/
